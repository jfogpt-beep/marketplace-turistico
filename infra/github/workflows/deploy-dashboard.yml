name: Deploy Dashboard (Panel Agencia/Admin)

on:
  push:
    branches: [main, develop]
    paths:
      - 'dashboard/**'
      - 'shared/**'
      - '.github/workflows/deploy-dashboard.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        default: 'staging'
        type: choice
        options:
          - dev
          - staging
          - prod

env:
  NODE_VERSION: '22'
  NEXT_PUBLIC_API_URL: ${{ github.ref == 'refs/heads/main' && 'https://api.marketplace-turistico.workers.dev' || 'https://marketplace-api-staging.workers.dev' }}

jobs:
  build-and-deploy:
    name: Build & Deploy Dashboard
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event.inputs.environment || (github.ref == 'refs/heads/main' && 'prod' || 'staging') }}
      url: ${{ steps.deploy.outputs.url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint -- --workspace=dashboard

      - name: Type check
        run: npm run typecheck:dashboard

      - name: Build Dashboard
        run: npm run build:dashboard
        env:
          NEXT_PUBLIC_API_URL: ${{ env.NEXT_PUBLIC_API_URL }}
          NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: ${{ secrets.STRIPE_PUBLISHABLE_KEY }}

      - name: Deploy to Cloudflare Pages
        id: deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: './dashboard'
          command: pages deploy .next --project-name="marketplace-dashboard-${{ github.event.inputs.environment || (github.ref == 'refs/heads/main' && 'prod' || 'staging') }}"

      - name: Post-deploy notification
        if: always()
        run: |
          echo "✅ Dashboard deployed successfully!"
          echo "URL: ${{ steps.deploy.outputs.deployment-url }}"
